# 性能优化检查清单

按难易程度排序的优化方法参考。

## 优化检查项

### 1. 数据结构与算法
- [ ] 是否采用高效数据结构
- [ ] 表达式转换优化（例如 `5 < val && val < 34` 可以用偏移后无符号比较来替换）

### 2. 多线程与并发
- [ ] 是否有多个线程访问同一个变量或者相邻变量（注意伪共享）
- [ ] 是否设置了线程亲和性（核亲和）
- [ ] 是否并行化 loop versioning
- [ ] 改完是否有 bug，比如内存屏障相关的"百万美元问题"

### 3. CPU 相关优化
- [ ] 是否有 CPU 分支预测失败概率高（可用 `perf stat -d -d -d` 检测）
- [ ] 指针是否因为别名分析导致汇编冗余

### 4. 工具使用
- [ ] 使用 PGO（Profile-Guided Optimization）
- [ ] 使用 BOLT/Propeller 等二进制优化工具
