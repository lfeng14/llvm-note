##### 优化方法按照难易程度
- 是否多线程访问同一个变量或者相邻变量
- 是否并行化 loop versioning
- 是否核亲和
- 是否采用高效数据结构
- 改完是否有bug，比如百万美元问题
- 是否有cpu分支预测失败概率高(perf stat -d -d -d)
- 指针是否因为别名分析导致汇编冗余
- 表达式转换比如5<val and val < 34可以用偏移后无符号比较来替换(负数值比较大)
- 
