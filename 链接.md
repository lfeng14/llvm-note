- 静态链接，输入文件顺序比较关键，否则容易出现符号找不到问题
- 重定位条目relocatioin entry
- 计算从下一条指令的PC值到目标符号的偏移量时，使用公式 (S + A - P)，其中S为目标符号地址，A为修正值（addend），P为当前指令的PC值。因此，需要加上addend作为修正项。
  
  <img width="500" height="250" alt="image" src="https://github.com/user-attachments/assets/a8dad7de-d554-47f2-af01-3226a797a8c8" />
